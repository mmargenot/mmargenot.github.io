To run the server locally, navigate to the root and run:

```
hugo server
```

To also see the draft posts, you can run:

```
hugo server -D
```

Running the server generates the public posts, so make sure the index and whatnot line up with what you want public _before_ you commit and push.
